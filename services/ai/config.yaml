APP_NAME: ai-service
APP_ENV: development

SERVER:
  HOST: 0.0.0.0
  PORT: 8010
  RELOAD: true

LOG:
  LEVEL: info

INTERNAL_TOKEN: ""
DATABASE_DSN: ""

HTTP:
  REQUEST_TIMEOUT_SECONDS: 120

MODEL:
  CONFIG_TABLE: dw_ai_models
  DEFAULT_CHAT_PATH: /chat/completions
  DEFAULT_CHAT_COMPLETIONS_PATH: /chat/completions
  DEFAULT_CHAT_RESPONSES_PATH: /responses
  DEFAULT_EMBEDDINGS_PATH: /embeddings
  DEFAULT_RERANK_PATH: /rerank
  DEFAULT_AUDIO_SPEECH_PATH: /audio/speech
  DEFAULT_AUDIO_TRANSCRIPTIONS_PATH: /audio/transcriptions
  DEFAULT_MODELS_PATH: /models
